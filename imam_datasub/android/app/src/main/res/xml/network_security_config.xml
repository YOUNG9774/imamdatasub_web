<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Development: allow local HTTP backend while testing on a real phone. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Production API domain with certificate pinning -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">alrahuzdata.com.ng</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
        <!-- Uncomment and add actual SHA-256 pins before Play Store release -->
        <!--
        <pin-set expiration="2026-01-01">
            <pin digest="SHA-256">REPLACE_WITH_ACTUAL_BASE64_PIN=</pin>
            <pin digest="SHA-256">REPLACE_WITH_BACKUP_BASE64_PIN=</pin>
        </pin-set>
        -->
    </domain-config>

    <!-- Firebase domains -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">firebaseapp.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">firebase.google.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Debug only - allow local backend hosts for dev tools -->
    <!-- Remove entirely in production builds -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.42.134</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
